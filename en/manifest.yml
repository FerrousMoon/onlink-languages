---
version: 1

Data:
  Common: common.yml
  Calendar: calendar.yml
  Hardware: hardware/common.yml
  VitalRecords: vital_records.yml

Computer: {!import computer/manifest.yml !}
Interfaces: {!import interface/manifest.yml !}
News: {!import news/manifest.yml !}
Messages: {!import message/manifest.yml !}
Missions: {!import mission/manifest.yml !}
Software: {!import software/manifest.yml !}
Hardware: {!import hardware/manifest.yml !}
